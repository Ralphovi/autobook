---
created: 26.07.2026 19:00
type: index
tags:
  - index
  - architecture
---
# Architecture overview

The living description of the system as it currently is — trimmed
[arc42](https://arc42.org/), numbered `NN-section-name.md`. Edited continuously;
it must never lag the code.

**This directory is empty, and correctly so.** There is no implemented system to
describe. Writing an architecture document now would produce a description of
something that does not exist, which is what
[proposal 0001](../proposals/draft/0001-gnucash-auto-booking-pipeline.md) is
already for.

The first sections land with the first code — build order step 2 — and from that
point "update the architecture doc" is a merge requirement on any PR that changes
structure, not a follow-up.

Start a section from [`../templates/architecture.md`](../templates/architecture.md).
